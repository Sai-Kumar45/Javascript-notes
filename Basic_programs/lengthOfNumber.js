let num = prompt();
let count = 0;
while (num !== 0) {
  num = parsient(num / 10);
  count++;
}
return count;
///
let number = 12345;
let length = numbercount(number);
console.log("the length of the number" + num + ":" + length);
